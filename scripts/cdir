#!/usr/bin/env bash

# Make sure Bash won't read be confused with this function with
# any other function or alias.
unset cdir

# Change current working directory to destination using cd(1),
# or create destination directories recursively if they do not
# exist, and then cd(1) to that destination.
# Note that previous working directory will be remembered so that
# popd(1) can be used to move working directory back into it.
# Note that mkdir(1) does not need to be inside an if statement
# to check if directory exists with -p option, so directory
# check is omitted.
function cdir {
    local destination="$1"
    mkdir --parents "$destination"
    pushd "$destination" >/dev/null
}

